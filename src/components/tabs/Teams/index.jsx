import { Empty } from '../../layout/Empty'

export function Teams() {
  return <Empty title="Squads is next up" body="Team/player editing with photo upload to Supabase Storage lands in the data-layer build phase." />
}
