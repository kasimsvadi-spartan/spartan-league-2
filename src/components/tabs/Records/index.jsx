import { Empty } from '../../layout/Empty'

export function Records() {
  return <Empty title="Records is next up" body="Auto-computed season records, head-to-head grid, and manual overrides land in their own build phase." />
}
