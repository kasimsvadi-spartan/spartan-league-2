import { Empty } from '../../layout/Empty'

export function PointsTable() {
  return <Empty title="Points Table is next up" body="Full standings, bonus breakdown, NRR, and the points-progression chart land in the scoring-core build phase." />
}
