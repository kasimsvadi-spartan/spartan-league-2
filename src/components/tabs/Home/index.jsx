import { Empty } from '../../layout/Empty'

export function Home() {
  return <Empty title="Home is next up" body="Announcements, the upcoming-slot card, countdown, and the prediction poll land in the next build phase." />
}
