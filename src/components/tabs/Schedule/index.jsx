import { Empty } from '../../layout/Empty'

export function Schedule() {
  return <Empty title="Schedule is next up" body="Slot creation, the 6-match auto-generation, and result entry land in the scoring-core build phase." />
}
